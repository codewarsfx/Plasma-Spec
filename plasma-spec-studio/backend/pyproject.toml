[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]
addopts = "-q -m 'not slow'"
markers = [
    "slow: tests that exercise the full multi-restart / Tvib fits and take 1+ minutes",
]

